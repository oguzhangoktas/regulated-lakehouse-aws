# Pinned to the AWS Glue 5.1 runtime: Spark 3.5.6, Python 3.11, Iceberg 1.10.0.
# Jobs are developed locally and run on Glue, so the local runtime matches the
# target rather than tracking latest.
pyspark==3.5.6
pandas==2.2.3
pyarrow==17.0.0
boto3==1.35.36
pytest==8.3.3
ruff==0.6.9
python-dotenv==1.0.1
build==1.2.2
pyyaml==6.0.2
confluent-kafka==2.5.3
jellyfish==1.1.0
