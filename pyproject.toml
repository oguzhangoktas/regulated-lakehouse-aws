[project]
name = "regulated-lakehouse"
version = "0.1.0"
requires-python = "==3.11.*"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["dataplatform*", "domains*"]

[tool.ruff]
line-length = 100
